
package Logica;

import persistencia.ControladoraPersistencia;

public class Controladora {
    
    ControladoraPersistencia controlPersistencia = new ControladoraPersistencia();
    
    public void crearUsuario(String nombreUsuario, String apellido, String correo, String contrasena, String telefono, String direccion, String tipoRol){
        
        /*Usuario user = new Usuario();
        user.setNombreUsuario(nombreUsuario);
        user.setContrasenia(contrasena);
        user.setTipoRol(tipoRol);
        user.setApellido(apellido);
        user.setCorreo(correo);
        user.setDireccion(direccion);
        user.setTelefono(telefono);
        controlPersistencia.crearUsuario(user);
   ¨*/
    }
    
}
